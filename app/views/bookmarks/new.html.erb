<%= simple_form_for [ @list, @bookmark ] do |f| %>
  <%= f.input :comment %>
  <%= f.association :list %>
  <%= f.association :movie %>
  <%= f.submit "Leave a comment:", class: "btn btn-primary" %>
<% end %>
<%= link_to "All lists", lists_path(@list)%>
